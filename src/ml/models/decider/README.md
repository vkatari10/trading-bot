# Decider

This is where the deciding ML model should be placed. Enure that it is a serialized ML Model that was dumped using pickle and contains the name `model.pkl`, this the ML model that will be exposed to the ML API
