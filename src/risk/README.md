# Risk 

This file contains C source code for account risk calculations that should be used in both the Python ML pipeline /Backtesters and Go runtime (risk) engine 

In order to use the C code in Python and Go you will need to use the provided `compile.sh` script that generates the **dynamic** library to link against. 


