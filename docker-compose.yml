services:
  ml_api:
    build: 
      context: .
      dockerfile: docker/Dockerfile.ml_api
    ports:
      - "5000:5000"
  
  runtime:
    build: 
      context: .
      dockerfile: docker/Dockerfile.runtime
    ports:
      - "6000:5000"
    depends_on:
      - ml_api
