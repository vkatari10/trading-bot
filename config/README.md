# Config Files

Place all config files in this folder, the `.env` treats this folder as its root so feel free to create new folders or paths, just be sure to adjust the values for the associated keys in the `.env` file.   

To learn more about creating your own config file, read more [here](../docs/CONFIG.md)